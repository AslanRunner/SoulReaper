/**
 * Defines a combat contract for characters that can attack a target.
 */
public interface Attack {
    /**
     * Defines a combat contract for characters that can attack a target.
     *
     * @param target the soul that receives the attack
     */
    public void attack(BaseSoul target);
}
