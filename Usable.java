/**
 * Defines objects that can apply an effect to a target character.
 */
public interface Usable {
    /**
     * Defines objects that can apply an effect to a target character.
     *
     * @param target the soul that receives the effect
     */
    public void use(BaseSoul target);
}
